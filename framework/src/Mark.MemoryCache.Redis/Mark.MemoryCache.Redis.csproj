<Project Sdk="Microsoft.NET.Sdk">

    <PropertyGroup>
        <ImplicitUsings>enable</ImplicitUsings>
        <Nullable>enable</Nullable>
    </PropertyGroup>

    <!-- NuGet Package Configuration -->
    <PropertyGroup>
        <PackageId>Mark.MemoryCache.Redis</PackageId>
        <Description>Redis-based memory cache implementation for the Mark framework</Description>
        <PackageTags>mark, framework, redis, memory-cache, caching, distributed-cache</PackageTags>
    </PropertyGroup>

    <ItemGroup>
      <ProjectReference Include="..\Mark.Core\Mark.Core.csproj" />
      <ProjectReference Include="..\Mark.MemoryCache.Abstractions\Mark.MemoryCache.Abstractions.csproj" />
      <ProjectReference Include="..\Mark.MemoryCache\Mark.MemoryCache.csproj" />
    </ItemGroup>

    <ItemGroup>
      <PackageReference Include="StackExchange.Redis" />
      <PackageReference Include="Microsoft.Extensions.Logging.Abstractions" />
      <PackageReference Include="Microsoft.Extensions.Options" />
      <PackageReference Include="Microsoft.Extensions.Hosting.Abstractions" />
      <PackageReference Include="System.Text.Json" />
    </ItemGroup>

</Project>
