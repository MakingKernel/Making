<Project Sdk="Microsoft.NET.Sdk">

    <PropertyGroup>
        <ImplicitUsings>enable</ImplicitUsings>
        <Nullable>enable</Nullable>
    </PropertyGroup>

    <!-- NuGet Package Configuration -->
    <PropertyGroup>
        <PackageId>Mark.MemoryCache</PackageId>
        <Description>In-memory cache implementation for the Mark framework</Description>
        <PackageTags>mark, framework, memory-cache, in-memory, caching</PackageTags>
    </PropertyGroup>

    <ItemGroup>
      <ProjectReference Include="..\Mark.Core\Mark.Core.csproj" />
      <ProjectReference Include="..\Mark.MemoryCache.Abstractions\Mark.MemoryCache.Abstractions.csproj" />
    </ItemGroup>

    <ItemGroup>
      <PackageReference Include="Microsoft.Extensions.Caching.Memory" />
      <PackageReference Include="Microsoft.Extensions.Logging.Abstractions" />
      <PackageReference Include="System.Text.Json" />
    </ItemGroup>

</Project>
