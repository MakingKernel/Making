<Project Sdk="Microsoft.NET.Sdk">

    <PropertyGroup>
        <ImplicitUsings>enable</ImplicitUsings>
        <Nullable>enable</Nullable>
    </PropertyGroup>

    <!-- NuGet Package Configuration -->
    <PropertyGroup>
        <PackageId>Mark.Security</PackageId>
        <Description>Security utilities and extensions for the Mark framework</Description>
        <PackageTags>mark, framework, security, authentication, authorization</PackageTags>
    </PropertyGroup>

    <ItemGroup>
      <ProjectReference Include="..\Mark.Core\Mark.Core.csproj" />
    </ItemGroup>

</Project>
