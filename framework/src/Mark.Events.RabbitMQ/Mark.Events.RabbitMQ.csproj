<Project Sdk="Microsoft.NET.Sdk">

    <PropertyGroup>
        <Nullable>enable</Nullable>
        <RootNamespace>Mark.Events.RabbitMQ</RootNamespace>
    </PropertyGroup>

    <!-- NuGet Package Configuration -->
    <PropertyGroup>
        <PackageId>Mark.Events.RabbitMQ</PackageId>
        <Description>RabbitMQ event bus implementation for the Mark framework</Description>
        <PackageTags>mark, framework, rabbitmq, events, messaging, event-bus</PackageTags>
    </PropertyGroup>

    <ItemGroup>
      <ProjectReference Include="..\Mark.Events\Mark.Events.csproj" />
      <ProjectReference Include="..\Mark.RabbitMQ\Mark.RabbitMQ.csproj" />
      <PackageReference Include="Microsoft.Extensions.DependencyInjection" />
      <PackageReference Include="Microsoft.Extensions.Hosting" />
      <PackageReference Include="RabbitMQ.Client" />
      <PackageReference Include="System.Text.Json" />
    </ItemGroup>

</Project>